# quizmd-docs

Nextra Docs Theme site for QuizMD.

## Local development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The static export is produced in `out/` and deployed via GitHub Actions to GitHub Pages.
